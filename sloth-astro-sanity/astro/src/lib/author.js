// Single named byline for the whole site. Every roundup is edited under this
// persona rather than published anonymously — same person on every article,
// not a per-document Sanity field, since it doesn't vary.
export const AUTHOR = {
  name: 'Jordan Ellison',
  title: 'Founding Editor, Shopping Sloth',
  bio: "Jordan started Shopping Sloth after one too many \"best of\" roundups that read like nobody had spent more than twenty minutes on them. Every pick here starts from real reviews, testing writeups, and return-rate data, gets cross-checked before it's trusted, and only makes the list once the cheap-looking option turns out to actually be cheap and not just cheap-looking.",
}
