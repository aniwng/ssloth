# Copy to .env in this directory. The Studio is bundled for the browser, so it
# can only read SANITY_STUDIO_-prefixed vars from its own directory — it will
# not pick up the .env one level up.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
